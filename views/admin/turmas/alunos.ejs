<%- include('../../partials/header') %>

<div class="container-fluid py-4">
  <div class="row mb-4">
    <div class="col-12">
      <h1>
        <i class="bi bi-people"></i> Alunos - <%= turma.nome %>
      </h1>
      <p class="text-muted">
        <i class="bi bi-calendar"></i> <%= turma.trimestre %>º Trimestre |
        <i class="bi bi-clock"></i> <%= turma.horario_inicio %> às <%= turma.horario_fim %>
      </p>
    </div>
  </div>

  <div class="card border-0 shadow-sm">
    <% if (alunos && alunos.length > 0) { %>
      <div class="table-responsive">
        <table class="table mb-0">
          <thead class="bg-light">
            <tr>
              <th>Nome</th>
              <th>Data Nascimento</th>
              <th>Responsável</th>
              <th>Telefone</th>
              <th>Status</th>
              <th style="width: 100px;">Ações</th>
            </tr>
          </thead>
          <tbody>
            <% alunos.forEach(aluno => { %>
              <tr>
                <td>
                  <strong><%= aluno.nome %></strong>
                </td>
                <td>
                  <% if (aluno.data_nascimento) { %>
                    <%= new Date(aluno.data_nascimento).toLocaleDateString('pt-BR') %>
                  <% } else { %>
                    <span class="text-muted">—</span>
                  <% } %>
                </td>
                <td>
                  <% if (aluno.responsavel) { %>
                    <%= aluno.responsavel %>
                  <% } else { %>
                    <span class="text-muted">—</span>
                  <% } %>
                </td>
                <td>
                  <% if (aluno.telefone_responsavel) { %>
                    <%= aluno.telefone_responsavel %>
                  <% } else { %>
                    <span class="text-muted">—</span>
                  <% } %>
                </td>
                <td>
                  <% if (aluno.ativa) { %>
                    <span class="badge bg-success">
                      <i class="bi bi-check-circle"></i> Ativo
                    </span>
                  <% } else { %>
                    <span class="badge bg-secondary">
                      <i class="bi bi-x-circle"></i> Inativo
                    </span>
                  <% } %>
                </td>
                <td>
                  <form method="POST" action="/admin/turmas/<%= turma.id %>/alunos/<%= aluno.id %>/toggle" style="display: inline;">
                    <input type="hidden" name="_csrf" value="<%= typeof csrfToken !== 'undefined' ? csrfToken : '' %>">
                    <button type="submit" class="btn btn-sm <%= aluno.ativa ? 'btn-outline-danger' : 'btn-outline-success' %>">
                      <% if (aluno.ativa) { %>
                        <i class="bi bi-x-circle"></i> Desativar
                      <% } else { %>
                        <i class="bi bi-check-circle"></i> Ativar
                      <% } %>
                    </button>
                  </form>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
      <div class="card-footer bg-light">
        <small class="text-muted">
          <i class="bi bi-info-circle"></i> Total de <%= alunos.length %> aluno(s) matriculado(s)
        </small>
      </div>
    <% } else { %>
      <div class="card-body">
        <p class="text-muted text-center py-5">
          <i class="bi bi-inbox" style="font-size: 3rem;"></i>
          <br>
          Nenhum aluno matriculado nesta turma
        </p>
      </div>
    <% } %>
  </div>

  <div class="mt-4">
    <a href="/admin/turmas/<%= turma.id %>/alunos/editar" class="btn btn-primary">
      <i class="bi bi-pencil"></i> Editar Alunos
    </a>
    <a href="/admin/turmas/<%= turma.id %>/matriculas" class="btn btn-outline-primary">
      <i class="bi bi-person-plus"></i> Gerenciar Matrículas (Legacy)
    </a>
    <a href="/admin/turmas" class="btn btn-secondary">
      <i class="bi bi-arrow-left"></i> Voltar
    </a>
  </div>
</div>

<%- include('../../partials/footer') %>
